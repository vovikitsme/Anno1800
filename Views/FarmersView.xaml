<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Anno1800.Views.FarmersView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Farmers">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">


            <Label FontSize="18" Text="Потребности фермеров:" />

            <CollectionView IsGrouped="True" ItemsSource="{Binding GroupedFarmerNeeds}">
                <CollectionView.GroupHeaderTemplate>
                    <DataTemplate>
                        <Label
                            Padding="10,5"
                            BackgroundColor="#f0f0f0"
                            FontAttributes="Bold"
                            FontSize="18"
                            Text="{Binding Title}"
                            TextColor="Black" />
                    </DataTemplate>
                </CollectionView.GroupHeaderTemplate>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout Padding="5" Spacing="10">
                            <Label FontSize="16" Text="{Binding Name}" />
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>



            <CollectionView ItemsSource="{Binding Needs}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                            <Image
                                Aspect="AspectFit"
                                HeightRequest="40"
                                Source="{Binding IconPath}"
                                WidthRequest="40" />

                            <VerticalStackLayout>
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="16"
                                    Text="{Binding Name}" />

                                <Label
                                    FontSize="14"
                                    Text="{Binding NeedTypeName}"
                                    TextColor="Gray" />
                            </VerticalStackLayout>
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
