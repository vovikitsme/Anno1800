<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Anno1800.Views.FarmersView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Farmers">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <Label
                FontAttributes="Bold"
                FontSize="22"
                HorizontalOptions="Center"
                Text="Фермеры" />

            <Label FontSize="18" Text="Количество фермеров:" />
            <Entry
                FontSize="16"
                HeightRequest="50"
                Keyboard="Numeric"
                Placeholder="Например: 100"
                Text="{Binding FarmersCount}" />

            <Button
                BackgroundColor="#5C27FE"
                Command="{Binding CalculateCommand}"
                CornerRadius="12"
                FontSize="18"
                HeightRequest="55"
                Text="Рассчитать"
                TextColor="White" />

            <Label
                Margin="0,20,0,0"
                FontSize="16"
                Text="{Binding CalculationResult}"
                TextColor="Black" />

            <!--  Вывод потребностей с иконками  -->
            <CollectionView ItemsSource="{Binding Needs}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid
                            Padding="10"
                            ColumnDefinitions="Auto,*"
                            RowDefinitions="Auto">
                            <Image
                                Margin="0,0,10,0"
                                HeightRequest="40"
                                Source="{Binding IconPath}"
                                WidthRequest="40" />
                            <VerticalStackLayout Grid.Column="1">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="16"
                                    Text="{Binding Name}" />
                                <Label
                                    FontSize="14"
                                    Text="{Binding NeedTypeName}"
                                    TextColor="Gray" />
                            </VerticalStackLayout>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
